# 当前行动：ThreadPool 收尾 → TCP Server

更新：2026-09-24（W3，09-20～09-26）。W1 起点为 2026-09-06。

## 已交付

- [x] 线程安全队列与单线程任务执行器
- [x] 多 worker、条件变量阻塞等待
- [x] submit / future 返回值及异常
- [x] 完成已接受任务后关闭；拒绝后续提交
- [x] README、测试、benchmark 与阶段复盘
- [x] 2026-09-24 独立验证 Debug 3/3 Passed、Release benchmark 校验通过

## W3 剩余时间：半天到一天巩固，再准备环境

- [ ] 不看实现，画出提交 → 排队 → worker 执行 → future 返回的流程。
- [ ] 解释等待谓词、锁外执行任务、stop 的退出条件。
- [ ] 在独立练习中写出一条“提交 → get → stop”的使用流程并运行；不要求背写完整线程池。
- [ ] 为下一项目确认 Linux/WSL 环境，验证 C++、CMake 和 TCP 客户端工具。
- [ ] 创建 concurrent-tcp-server 空仓库（当前尚未创建）。

不了解的点逐个写进 blockers.md；通过实际练习确认，不因旧 Issue 已关闭就视为熟练。

## W4（09-27～10-03）唯一交付：单连接 TCP echo

按顺序做，每次完成一项再开始下一项：

1. 解释客户端、服务端、端口和连接；画出 socket → bind → listen → accept → recv/send → close。
2. 自己实现监听与接受一个连接。
3. 实现收到什么字节就回传什么字节，处理实际读写长度与部分发送。
4. 客户端正常断开后关闭连接，服务端可以接受下一次连接。
5. 写自动验证脚本及最短运行说明。

验收：
- [ ] 在 Linux 环境中可重复构建和运行。
- [ ] 客户端发送 hello，收到完全相同的字节。
- [ ] 超过单次接收缓冲区的数据仍可完整回显。
- [ ] 客户端断开再连接时服务器仍可使用。
- [ ] 能解释监听 socket 与已连接 socket 的区别。

## 后续两周

W5：复用线程池处理多个连接，明确 socket 所有权和停止时如何解除阻塞。
W6：压测、失败率、吞吐/延迟报告与复盘。详细任务见 [项目计划](plans/concurrent-tcp-server.md)。

学习方式：本人先解释并动手；需要帮助时只请求当前一步的提示或审查。当前任务无需一次实现完整服务器。
